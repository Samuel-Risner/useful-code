# You should settle on these first before starting to code.

INIT_WIDTH   = 850
"""The initial width which the window will have when created."""
INIT_HEIGHT  = 480
"""The initial height which the window will have when created."""
MIN_WIDTH    = INIT_WIDTH
"""The minimum width the window can have."""
MIN_HEIGHT   = INIT_HEIGHT
"""The minimum height the window can have."""

RELATIVE_HEIGHT = INIT_WIDTH / INIT_HEIGHT
RELATIVE_WIDTH = INIT_HEIGHT / INIT_WIDTH